
  # ATM Basic System Design (Community)

  This is a code bundle for ATM Basic System Design (Community). The original project is available at https://www.figma.com/design/DD4ySiql84csmK26WxLoRh/ATM-Basic-System-Design--Community-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  